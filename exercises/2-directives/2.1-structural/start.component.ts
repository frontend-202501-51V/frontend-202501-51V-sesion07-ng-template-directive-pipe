import { Component } from '@angular/core';

@Component({
  selector: 'app-structural-start',
  template: `
    <!-- TODO: Implementa aquí tus ejemplos de *ngIf, *ngFor y *ngSwitch -->
    <!-- Usa el archivo HINTS.md si necesitas ayuda -->
  `
})
export class StructuralStartComponent {}
